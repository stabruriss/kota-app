{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Kota v2",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-badge-count",
    "core:window:allow-start-dragging",
    "shell:default",
    "fs:default",
    "dialog:default"
  ]
}
