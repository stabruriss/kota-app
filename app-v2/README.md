# app-v2

This directory is reserved for the Kota desktop app source release.

The application source is not included yet. It will be added in a later source
publication step after the release website, public documents, update manifest,
and signed download flow are ready.

Do not add installer artifacts here. Signed and notarized `.dmg` files belong in
GitHub Releases, not in git.
